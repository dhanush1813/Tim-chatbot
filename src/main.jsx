import React from "react";
import { createRoot } from "react-dom/client";
import TimChat from "../Tim_aldo.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TimChat />
  </React.StrictMode>
);
